node_modules/

# ── 运行时产生的，不进版本库 ──────────────────────────────
#
# config.json 里是你的 API key 和两把令牌 —— 这个绝对不能提交。
# 仓库里只有 config.example.json（占位符版本）。
config.json

# 真实数据：聊天记录、留言、日历、状态
data.json
data.json.tmp

# 运行日志与她上传的东西
relay.log
avatar.jpg
music/
gallery/
